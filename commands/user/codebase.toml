# Clean and refactor codebase

name = "clean:codebase"
description = "Clean and refactor codebase"
prompt = """
# Core Cleanup
Remove unused imports, variables, and dead code.
Delete commented or abandoned code.
Detect repeated logic and merge it.
Keep all behavior, APIs, and routes stable.

# Readability and Refactor
Break large functions into smaller parts.
Rename confusing functions, variables, and files.
Improve directory layout for clear navigation.
Make error messages direct and easy to follow.
"""
