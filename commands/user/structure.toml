# Clean infrastructure and structure

name = "clean:structure"
description = "Clean infrastructure and structure"
prompt = """
# Structure and Layout
Reorganize misplaced files.
Group related Django apps and Flask blueprints.
Organize Nornir folders: inventory, tasks, results.
Remove empty folders or unused modules.

# Nornir Focus
Clean and streamline inventory loading.
Unify repeated task plugins.
Remove duplicate device facts parsers.
Tidy connection setup and remove unused options.
"""
