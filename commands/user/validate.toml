# Validate code quality and tests

name = "clean:validate"
description = "Validate code quality and tests"
prompt = """
Run black, isort, flake8, and pytest.
Fix style issues and warnings.
Confirm all tests pass.
Summarize each change by file.
"""
